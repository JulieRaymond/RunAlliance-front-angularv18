server {
  listen 80;
  server_name run-alliance.wcs-students.fr;

  root /usr/share/nginx/html; # Correspond à la COPY du Dockerfile
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}